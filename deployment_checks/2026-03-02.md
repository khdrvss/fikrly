Latest automated check — 2026-03-02

- Check run: `python3 manage.py check` from project root
- Result: FAILED — Django not available in the active environment

  - Error excerpt: ModuleNotFoundError: No module named 'django'
  - Cause: No active virtual environment with project dependencies, or Django not installed in the system Python.

- Migrations: Not executed (skipped due to failed system check)

Recommended next steps

1. Activate the project's virtual environment (if available) and re-run checks:

   ```bash
   source .venv/bin/activate
   python manage.py check
   python manage.py migrate --noinput
   ```

2. If you use Docker for deployment, run the checks inside the container:

   ```bash
   docker-compose -f docker-compose.prod.yml up -d
   docker-compose -f docker-compose.prod.yml exec web python manage.py check
   ```

3. If you want, I can run these steps — tell me which environment to use: `system`, `.venv`, or `docker`.
