version: '3.8'

# Development override for docker-compose.yml
# Usage: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  db:
    ports:
      - "5435:5432"  # Changed to avoid conflict with co-found-dev-db

  redis:
    ports:
      - "6381:6379"  # Changed to avoid conflict with co-found-dev-redis

  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder  # Use builder stage for development
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app  # Mount entire project for hot reload
    environment:
      - DEBUG=True
      - SILK_ENABLED=True
      - EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
    ports:
      - "8000:8000"  # Expose Django directly
    stdin_open: true
    tty: true

  # Disable Nginx in development (access Django directly)
  nginx:
    profiles:
      - production
